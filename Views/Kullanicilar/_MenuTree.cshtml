@model OgrenciBilgiSistemi.ViewModels.MenuOgeAssignmentVm
@{
    // Unique id
    var cid = $"menu_{Model.MenuOgeId}";
}

<li class="mb-1">
    <details class="menu-node" @(Model.Children?.Any() == true ? "" : "open")>
        <summary class="d-flex align-items-center gap-2">
            <input class="form-check-input"
                   type="checkbox"
                   id="@cid"
                   data-menu-id="@Model.MenuOgeId"
                   data-parent-id="@(Model.AnaMenuId?.ToString() ?? "")"
                   @(Model.IsAssigned ? "checked" : "") />
            <label class="form-check-label" for="@cid">@Model.Title</label>
        </summary>

        @if (Model.Children != null && Model.Children.Count > 0)
        {
            <ul class="list-unstyled ms-4 mt-1">
                @foreach (var child in Model.Children)
                {
                    @Html.Partial("_MenuTree", child)
                }
            </ul>
        }
    </details>
</li>