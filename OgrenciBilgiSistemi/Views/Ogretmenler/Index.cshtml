@model PaginatedListModel<OgrenciBilgiSistemi.Models.OgretmenModel>

@{
    ViewData["Title"] = "Öğretmen İşlemleri";
    var q = ViewData["CurrentFilter"] as string;
    var page = Model.PageIndex;
    var total = Model.TotalPages;
    var count = Model.TotalCount;
}

<div class="viewport-scroller">
    <div class="fixed-canvas">
        <div class="card border rounded-4 p-3 p-md-4 mt-4">

            <!-- Başlık + aksiyonlar -->
            <div class="d-flex justify-content-between align-items-center mb-4 flex-nowrap no-wrap-scroll">
                <h3 class="text-primary fw-bold mb-0">@ViewData["Title"]</h3>

                <a class="btn btn-success fw-semibold"
                   asp-controller="Ogretmenler"
                   asp-action="Ekle">
                    <i class="bi bi-plus-lg"></i> Öğretmen Ekle
                </a>
            </div>

            <!-- Filtre Formu -->
            <form method="get"
                  asp-controller="Ogretmenler" asp-action="Index"
                  class="d-flex align-items-end gap-3 flex-nowrap no-wrap-scroll w-100 bg-body-tertiary border rounded-3 p-3 mb-3">

                <!-- Öğretmen Arama -->
                <div>
                    <label for="searchString" class="visually-hidden">Öğretmen</label>
                    <div class="input-group">
                        <span class="input-group-text bg-white"><i class="bi bi-search"></i></span>
                        <input type="text" id="searchString" name="searchString"
                               class="form-control" placeholder="Ad, soyad veya kart no ara…"
                               value="@q" style="width:280px;" />
                    </div>
                </div>

                <!-- Ara -->
                <div class="ms-auto">
                    <button class="btn btn-primary px-3 text-nowrap" type="submit">
                        <i class="bi bi-search me-1"></i> Ara
                    </button>
                </div>
            </form>

            <!-- Liste -->
            @if (Model == null || !Model.Any())
            {
                <div class="alert alert-warning mb-0">
                    <i class="bi bi-exclamation-triangle-fill"></i> Kayıt bulunamadı.
                </div>
            }
            else
            {
                <div class="table-scroll">
                    <table class="table table-hover table-sm align-middle">
                        <thead class="bg-dark text-white">
                            <tr class="text-center">
                                <th class="text-start">Ad Soyad</th>
                                <th>Durum</th>
                                <th>Birimi</th>
                                <th>Fotoğraf</th>
                                <th>Kart No</th>
                                <th>İşlem</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var s in Model)
                            {
                                <tr class="text-center">
                                    <td class="text-start fw-semibold text-primary">@s.OgretmenAdSoyad</td>
                                    <td>
                                        <span class="badge @(s.OgretmenDurum ? "bg-success" : "bg-danger")">
                                            @(s.OgretmenDurum ? "Aktif" : "Pasif")
                                        </span>
                                    </td>
                                    <td>@(s.Birim?.BirimAd ?? "Birim Yok")</td>
                                    <td>
                                        <img src="@(!string.IsNullOrEmpty(s.OgretmenGorsel) ? s.OgretmenGorsel : "/images/default.png")"
                                             alt="Foto" class="rounded-circle border" width="48" height="48" />
                                    </td>
                                    <td>@(s.OgretmenKartNo ?? "-")</td>
                                    <td>
                                        <div class="btn-group btn-group-gap" role="group" style="min-width: 200px;">
                                            <a asp-controller="Ogretmenler" asp-action="Detay" asp-route-id="@s.OgretmenId"
                                               class="btn btn-outline-info btn-sm">
                                                <i class="bi bi-eye"></i> Detay
                                            </a>
                                            <a asp-controller="Ogretmenler" asp-action="Guncelle" asp-route-id="@s.OgretmenId"
                                               class="btn btn-primary btn-sm">
                                                <i class="bi bi-pencil-square"></i> Güncelle
                                            </a>
                                            <button type="button"
                                                    class="btn btn-danger btn-sm"
                                                    data-bs-toggle="modal"
                                                    data-bs-target="#silModal"
                                                    onclick="document.getElementById('ogretmenId').value='@s.OgretmenId'">
                                                <i class="bi bi-trash"></i> Sil
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                <!-- Sayfalama -->
                <div class="d-flex justify-content-between align-items-center mt-3">
                    <div class="text-muted">
                        Toplam <strong>@count</strong> kayıt • Sayfa <strong>@(page)</strong>/<strong>@(total)</strong>
                    </div>
                    <div class="btn-group btn-group-gap">
                        <a class="btn btn-outline-secondary @(Model.HasPreviousPage ? "" : "disabled")"
                           asp-action="Index"
                           asp-route-searchString="@q"
                           asp-route-page="@(page - 1)">
                            ‹ Önceki
                        </a>

                        <a class="btn btn-outline-secondary @(Model.HasNextPage ? "" : "disabled")"
                           asp-action="Index"
                           asp-route-searchString="@q"
                           asp-route-page="@(page + 1)">
                            Sonraki ›
                        </a>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

<!-- Silme Modal -->
<div class="modal fade" id="silModal" tabindex="-1" aria-labelledby="silModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="silModalLabel">Öğretmen Sil</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Bu öğretmeni silmek istediğinize emin misiniz?</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">İptal</button>
                <form id="silForm" method="post" asp-action="Sil">
                    @Html.AntiForgeryToken()
                    <input type="hidden" id="ogretmenId" name="id" />
                    <button type="submit" class="btn btn-danger">Evet, Sil!</button>
                </form>
            </div>
        </div>
    </div>
</div>
